# Environment for the JupyterLab container.
#
# Identical pins to requirements.txt, which the BonsAI service uses. The notebook and the
# service import the same src/ modules, so they must agree on the libraries underneath —
# a notebook that scores prompts with a different openai or mlflow than the one serving
# them is measuring something slightly different from what customers get.

flask==3.1.3
openai==3.17.0
requests==2.34.2

mlflow==3.16.1
pandas==3.0.6
numpy==2.5.3
matplotlib==3.11.2

rouge-score==0.1.2

# JupyterLab progress bars; without it MLflow prints a tqdm/IProgress warning.
ipywidgets==8.1.7
